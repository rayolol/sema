
[package]
name = "sema"
version = "0.1.0"
edition = "2024"

[lib]
name = "sema"
path = "src/lib.rs"

[dependencies]
anyhow = "1.0.103"
resolve = { path = "resolve" }

syn = { version = "2.0", features = ["full", "derive"] }
quote = "1.0"

